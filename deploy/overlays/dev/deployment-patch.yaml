apiVersion: apps/v1
kind: Deployment
metadata:
  name: polaris-app
spec:
  template:
    metadata:
      labels:
        app.kubernetes.io/version: v0.1.0
    spec:
      containers:
        - name: polaris-app
          imagePullPolicy: IfNotPresent
