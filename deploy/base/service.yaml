apiVersion: v1
kind: Service
metadata:
  name: polaris-app
  labels:
    app: polaris-app
spec:
  type: ClusterIP
  selector:
    app: polaris-app
  ports:
    - name: http
      port: 80
      targetPort: http
